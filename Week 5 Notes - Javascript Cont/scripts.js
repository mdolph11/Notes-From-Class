// Basic "If" statements 
// Make it to where the example checks "if" the number is positive
let number = 10;
if (number > 0);
console.log("The number is positive."); // Output: the "if" statement of var "let" make its true/positive.

